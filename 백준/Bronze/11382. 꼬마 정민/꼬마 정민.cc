#include <iostream>
#include <algorithm>
#include <vector>
#include <cmath>

#define FAST ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL)

using namespace std;


int main() {
    FAST;
    long long a, b, c;
    cin >> a >> b >> c;
    cout << (long long)(a + b + c);
    return 0;
}
